spring:
  application:
    name: floorpdv

  datasource:
    url: jdbc:postgresql://localhost:5432/pdv
    username: postgres
    password: 110505

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true